#include <stdio.h>
#include <stdlib.h>

void selection_sort(int n, int vet[])
{
  if (n==1)
  {
    return v[n];
    else
    (v[n] < selection_sort(n-1;vet)
    troca (v[n],vet);
  }
}

int main()
{
  int n=10;
  int vet[];
  selection_sort (n,vet);
  
  return 0;
}
